import React from 'react';
import {Text,StyleSheet} from 'react-native';

export default StyleSheet.create({
    default: {
        fontSize: 15,
        color: '#000000',
        fontFamily: 'normal',
        textAlign:'center'
    },
});